## 1. Flip Guard Implementation

- [x] 1.1 Update `flip` in `cube.c` to return `NULL` when `isFlippable(cube)` is false
- [x] 1.2 Keep existing `NULL` input guard and valid-flip behavior unchanged

## 2. Test Coverage

- [x] 2.1 Add test: non-flippable cube input returns `NULL` from `flip`
- [x] 2.2 Keep existing positive flip tests passing for flippable cubes
- [x] 2.3 Run `make test` and confirm all tests pass
